
using UnityEngine;

public class Perk : MonoBehaviour
{
    /*
    [SerializeField] private int state_idx = 0;
    private readonly float perkSpeed = 2f;

    void FixedUpdate()
    {
        Vector2 _velocity = perkSpeed * Time.deltaTime * Vector2.down;
        transform.position += new Vector3(_velocity.x, _velocity.y, 0);
        if (transform.position.y < -10)
            Destroy(gameObject);
    }
    */
}
